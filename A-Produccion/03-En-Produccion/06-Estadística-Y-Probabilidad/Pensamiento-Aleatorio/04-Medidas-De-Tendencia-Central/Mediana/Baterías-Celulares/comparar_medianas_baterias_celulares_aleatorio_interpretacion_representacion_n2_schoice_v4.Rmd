---
output:
  html_document: default
  pdf_document: default
  word_document: default
---
```{r data generation, echo = FALSE, results = "hide"}
library(exams)

# Generación de nombres aleatorios para modelos
modelos <- sample(c("Modelo A", "Modelo B", "Modelo C", "Modelo D", "Modelo E",
                    "Modelo F", "Modelo G", "Modelo H", "Modelo J", "Modelo K",
                    "Modelo L", "Modelo M", "Modelo N", "Modelo P", "Modelo Q",
                    "Modelo R", "Modelo S", "Modelo T", "Modelo U", "Modelo V",
                    "Modelo W"), 3)

# Número aleatorio de pruebas entre 5 y 8
num_pruebas <- sample(5:8, 1)

# Generación de datos base para cada modelo
base_valores <- sample(500:800, 3 * num_pruebas, replace = TRUE)
matriz_datos <- matrix(base_valores, nrow = 3, ncol = num_pruebas)

# Relación deseada: med(Z) > med(X) y med(Z) > med(Y)
max_iter <- 500

mediana_deseada_x <- median(matriz_datos[1,])
mediana_deseada_y <- median(matriz_datos[2,])

# Aseguramos que Y tenga una mediana diferente a X
iter <- 0
while (mediana_deseada_y == mediana_deseada_x && iter < max_iter) {
  matriz_datos[2,] <- matriz_datos[2,] + sample(-5:5, num_pruebas, replace = TRUE)
  mediana_deseada_y <- median(matriz_datos[2,])
  iter <- iter + 1
}
if (iter >= max_iter) {
  posicion_mediana <- ceiling(num_pruebas / 2)
  matriz_datos[2, posicion_mediana] <- matriz_datos[2, posicion_mediana] + sample(c(-10, 10), 1)
  mediana_deseada_y <- median(matriz_datos[2,])
}

# Aseguramos que Z tenga mediana mayor que X e Y
matriz_datos[3,] <- sort(matriz_datos[3,])
max_mediana_xy <- max(mediana_deseada_x, mediana_deseada_y)
posicion_mediana <- ceiling(num_pruebas / 2)
matriz_datos[3, posicion_mediana] <- max_mediana_xy + sample(5:15, 1)
if (posicion_mediana < num_pruebas) {
  n_sup <- num_pruebas - posicion_mediana
  matriz_datos[3, (posicion_mediana + 1):num_pruebas] <- matriz_datos[3, (posicion_mediana + 1):num_pruebas] + sample(10:20, n_sup, replace = TRUE)
}
matriz_datos[3,] <- sample(matriz_datos[3,])

mediana_deseada_z <- median(matriz_datos[3,])
iter <- 0
while (mediana_deseada_z <= max_mediana_xy && iter < max_iter) {
  matriz_datos[3,] <- matriz_datos[3,] + 5
  mediana_deseada_z <- median(matriz_datos[3,])
  iter <- iter + 1
}

# Dos valores iguales en dos modelos diferentes (distractor de dificultad)
modelos_para_igualar <- sample(1:3, 2)
posicion_igual <- sample(1:num_pruebas, 1)
valor_comun <- matriz_datos[modelos_para_igualar[1], posicion_igual]
matriz_datos[modelos_para_igualar[2], posicion_igual] <- valor_comun

# Verificamos que las medianas sigan cumpliendo Z > X, Z > Y
mediana_deseada_x <- median(matriz_datos[1,])
mediana_deseada_y <- median(matriz_datos[2,])
mediana_deseada_z <- median(matriz_datos[3,])

if (mediana_deseada_z <= max(mediana_deseada_x, mediana_deseada_y)) {
  ajuste <- max(mediana_deseada_x, mediana_deseada_y) - mediana_deseada_z + sample(5:10, 1)
  matriz_datos[3,] <- matriz_datos[3,] + ajuste
}

# Tabla y medianas finales
tabla <- data.frame(
  Prueba = paste("Prueba", 1:num_pruebas),
  Modelo1 = matriz_datos[1,],
  Modelo2 = matriz_datos[2,],
  Modelo3 = matriz_datos[3,]
)

med1 <- median(matriz_datos[1,])
med2 <- median(matriz_datos[2,])
med3 <- median(matriz_datos[3,])

# Contexto narrativo
marcas <- c("SmartCell", "TechLife", "PowerMobile", "CellTech", "MobilPro")
marca_seleccionada <- sample(marcas, 1)
pruebas <- c("rendimiento", "uso intensivo", "resistencia", "durabilidad", "eficiencia")
prueba_seleccionada <- sample(pruebas, 1)
```

Question
========
La empresa `r marca_seleccionada` realizó pruebas de `r prueba_seleccionada` en tres modelos de teléfonos celulares. La siguiente tabla muestra la duración de la batería (en minutos) para cada modelo durante `r num_pruebas` pruebas diferentes:

```{r, echo = FALSE, results = "asis"}
indices_aleatorios <- sample(1:3)
modelos_aleatorios <- modelos[indices_aleatorios]
tabla_reordenada <- tabla[, c(1, indices_aleatorios + 1)]
knitr::kable(tabla_reordenada, col.names = c("", modelos_aleatorios))

indice_x <- which(indices_aleatorios == 1)
indice_y <- which(indices_aleatorios == 2)
indice_z <- which(indices_aleatorios == 3)
```

Al comparar la mediana de las duraciones de la batería de los tres modelos, ¿cuál de las siguientes afirmaciones es correcta?

Answerlist
----------
* El `r modelos_aleatorios[indice_z]` tiene una mediana igual a la del `r modelos_aleatorios[indice_y]` y menor que la del `r modelos_aleatorios[indice_x]`.
* El `r modelos_aleatorios[indice_z]` tiene una mediana mayor que la del `r modelos_aleatorios[indice_y]` y que la del `r modelos_aleatorios[indice_x]`.
* El `r modelos_aleatorios[indice_z]` tiene una mediana igual a la del `r modelos_aleatorios[indice_x]` y menor que la del `r modelos_aleatorios[indice_y]`.
* El `r modelos_aleatorios[indice_z]` tiene una mediana mayor que la del `r modelos_aleatorios[indice_x]` y menor que la del `r modelos_aleatorios[indice_y]`.

Solution
========
Para resolver este problema, ordenamos los datos de cada modelo y calculamos la mediana (valor central de los datos ordenados):

* **`r modelos_aleatorios[indice_x]`**: `r sort(matriz_datos[1,])` $\rightarrow$ Mediana = **`r med1`**
* **`r modelos_aleatorios[indice_y]`**: `r sort(matriz_datos[2,])` $\rightarrow$ Mediana = **`r med2`**
* **`r modelos_aleatorios[indice_z]`**: `r sort(matriz_datos[3,])` $\rightarrow$ Mediana = **`r med3`**

**Respuesta correcta:** El `r modelos_aleatorios[indice_z]` tiene una mediana mayor que la del `r modelos_aleatorios[indice_y]` y que la del `r modelos_aleatorios[indice_x]`, ya que `r med3` > `r med2` y `r med3` > `r med1`.

**Análisis de las opciones incorrectas:**

* *"Mediana igual a la del `r modelos_aleatorios[indice_y]` y menor que la del `r modelos_aleatorios[indice_x]`"*: Incorrecto. La mediana de `r modelos_aleatorios[indice_z]` (`r med3`) no es igual a la de `r modelos_aleatorios[indice_y]` (`r med2`), y tampoco es menor que la de `r modelos_aleatorios[indice_x]` (`r med1`); es mayor que ambas.

* *"Mediana igual a la del `r modelos_aleatorios[indice_x]` y menor que la del `r modelos_aleatorios[indice_y]`"*: Incorrecto. La mediana de `r modelos_aleatorios[indice_z]` (`r med3`) no es igual a la de `r modelos_aleatorios[indice_x]` (`r med1`); de hecho, es mayor.

* *"Mediana mayor que la del `r modelos_aleatorios[indice_x]` y menor que la del `r modelos_aleatorios[indice_y]`"*: Incorrecto. Si bien `r med3` > `r med1`, la mediana de `r modelos_aleatorios[indice_z]` no es menor sino mayor que la de `r modelos_aleatorios[indice_y]` (`r med3` > `r med2`).

Meta-information
============
exname: comparar_medianas_baterias_celulares_aleatorio_interpretacion_representacion_n2_schoice_v4
extype: schoice
exsolution: 0100
exshuffle: TRUE
extol: 0.01
exextra[Type]: SCHOICE
exextra[Competencia]: Interpretacion
exextra[Componente]: Aleatorio
exextra[Afirmacion]: Compara medidas de tendencia central entre conjuntos de datos
exextra[Evidencia]: Calcula medianas y establece relaciones de orden entre ellas
exextra[Nivel]: 2
