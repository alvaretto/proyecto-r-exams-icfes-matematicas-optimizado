---
output:
  pdf_document: default
  word_document: default
  html_document: default
---

```{r setup, include=FALSE}
# Configuración para todos los formatos de salida
Sys.setlocale(category = "LC_NUMERIC", locale = "C")
options(OutDec = ".")

# Configurar el motor LaTeX globalmente
options(tikzLatex = "pdflatex")
options(tikzXelatex = FALSE)
options(tikzLatexPackages = c(
  "\\usepackage{tikz}",
  "\\usepackage{colortbl}",
  "\\usepackage{amsmath}",
  "\\usepackage{array}",
  "\\usepackage{xcolor}",
  "\\usepackage{graphicx}",
  "\\usepackage{float}",
  "\\usetikzlibrary{babel,calc,3d}"
))

library(exams)
library(digest)
library(testthat)

typ <- match_exams_device()
options(scipen = 999)
knitr::opts_chunk$set(
  warning = FALSE,
  message = FALSE,
  fig.showtext = FALSE,
  fig.cap = "",
  fig.keep = 'all',
  dev = c("png", "pdf"),
  dpi = 150
)
```

```{r data_generation, echo=FALSE, results="hide"}
# Configuración de formato numérico estándar
options(OutDec = ".")
options(scipen = 999)
options(digits = 10)

# Establecer semilla aleatoria para reproducibilidad
set.seed(sample(1:100000, 1))

# Función principal de generación de datos
generar_datos <- function() {
  # NUEVA IMPLEMENTACIÓN: Aleatorización de catetos manteniendo filosofía ICFES
  # Objetivo: Generar valores que produzcan cálculos simples sin calculadora

  # Generar semilla única para esta ejecución
  timestamp_seed <- as.numeric(Sys.time()) * 1000000
  base_seed <- sample(1:1000000, 1)
  unique_seed <- (timestamp_seed + base_seed) %% 1000000

  # Aleatorizar contextos del problema para mayor diversidad
  contextos_problema <- c(
    "Margarita debe encontrar el valor del cateto x en el triángulo rectángulo que se muestra en la figura",
    "Carlos necesita determinar el valor del cateto x del triángulo rectángulo representado en el diagrama",
    "Ana debe calcular el valor del cateto x en el triángulo rectángulo que aparece en la imagen",
    "Luis requiere hallar el valor del cateto x del triángulo rectángulo mostrado en la figura",
    "María debe encontrar el valor del cateto x en el triángulo rectángulo que se presenta en el gráfico",
    "Sofía necesita calcular el valor del cateto x en el triángulo rectángulo de la figura",
    "Diego debe determinar el valor del cateto x del triángulo rectángulo mostrado",
    "Elena requiere encontrar el valor del cateto x en el triángulo rectángulo del diagrama",
    "Pablo debe hallar el valor del cateto x del triángulo rectángulo representado",
    "Carmen necesita calcular el valor del cateto x en el triángulo rectángulo de la imagen"
  )
  contexto_seleccionado <- sample(contextos_problema, 1)

  # NUEVA ALEATORIZACIÓN: Generar pares de catetos que produzcan hipotenusas simples
  # Filosofía ICFES: Cálculos manejables mentalmente, números enteros o decimales simples

  # Definir conjuntos de ternas pitagóricas y variaciones simples
  ternas_pitagoricas <- list(
    # Ternas pitagóricas básicas (a, b, c) donde c² = a² + b²
    list(a = 3, b = 4, c = 5),
    list(a = 5, b = 12, c = 13),
    list(a = 8, b = 15, c = 17),
    list(a = 7, b = 24, c = 25),
    list(a = 9, b = 12, c = 15),
    list(a = 6, b = 8, c = 10),

    # Múltiplos de ternas básicas (divididos para mantener simplicidad)
    list(a = 1.5, b = 2, c = 2.5),    # (3,4,5)/2
    list(a = 6, b = 8, c = 10),       # (3,4,5)*2
    list(a = 2.5, b = 6, c = 6.5),    # (5,12,13)/2

    # Casos especiales con raíces simples
    list(a = 1, b = 1, c = sqrt(2)),
    list(a = 2, b = 2, c = sqrt(8)),
    list(a = 1, b = sqrt(3), c = 2),
    list(a = sqrt(3), b = 1, c = 2),

    # Casos con decimales simples
    list(a = 1.2, b = 1.6, c = 2),
    list(a = 2.4, b = 3.2, c = 4),
    list(a = 0.6, b = 0.8, c = 1)
  )

  # Seleccionar una terna aleatoriamente
  terna_seleccionada <- sample(ternas_pitagoricas, 1)[[1]]

  # Decidir aleatoriamente cuál cateto será el conocido y cuál el desconocido
  configuracion <- sample(c("a_conocido", "b_conocido"), 1)

  if (configuracion == "a_conocido") {
    cateto_conocido <- terna_seleccionada$a
    cateto_desconocido <- terna_seleccionada$b  # Este será x
    hipotenusa <- terna_seleccionada$c
  } else {
    cateto_conocido <- terna_seleccionada$b
    cateto_desconocido <- terna_seleccionada$a  # Este será x
    hipotenusa <- terna_seleccionada$c
  }

  # Aleatorizar tipo de representación matemática para diversidad
  tipo_representacion <- sample(c("exacta", "decimal", "fraccionaria"), 1)

  # Aleatorizar factores de diversidad para generar 300+ versiones
  factor_diversidad <- sample(1:1000, 1)  # Factor ampliado
  variacion_contexto <- sample(1:100, 1)  # Variación ampliada
  seed_distractores <- unique_seed + factor_diversidad + variacion_contexto

  # Función de formateo inteligente para diferentes tipos de números
  formato_numero <- function(x, tipo_rep = "exacta") {
    # Números enteros
    if (abs(x - round(x)) < 0.001 && x <= 20) {
      return(as.character(round(x)))
    }

    # Decimales simples (una cifra decimal)
    if (abs(x - round(x, 1)) < 0.001 && x < 10) {
      return(as.character(round(x, 1)))
    }

    # Fracciones comunes
    if (abs(x - 1/2) < 0.001) return("\\frac{1}{2}")
    if (abs(x - 3/2) < 0.001) return("\\frac{3}{2}")
    if (abs(x - 5/2) < 0.001) return("\\frac{5}{2}")
    if (abs(x - 2/3) < 0.001) return("\\frac{2}{3}")
    if (abs(x - 4/3) < 0.001) return("\\frac{4}{3}")
    if (abs(x - 3/4) < 0.001) return("\\frac{3}{4}")
    if (abs(x - 5/4) < 0.001) return("\\frac{5}{4}")

    # Raíces comunes
    if (abs(x - sqrt(2)) < 0.001) return("\\sqrt{2}")
    if (abs(x - sqrt(3)) < 0.001) return("\\sqrt{3}")
    if (abs(x - sqrt(5)) < 0.001) return("\\sqrt{5}")
    if (abs(x - sqrt(8)) < 0.001) return("2\\sqrt{2}")

    # Formato por defecto
    return(as.character(round(x, 2)))
  }

  # Formatear valores principales
  cateto_conocido_tex <- formato_numero(cateto_conocido)
  cateto_desconocido_tex <- formato_numero(cateto_desconocido)
  hipotenusa_tex <- formato_numero(hipotenusa)

  # Usar seed_distractores para crear variaciones únicas
  set.seed(seed_distractores)

  # NUEVA GENERACIÓN DE DISTRACTORES BASADA EN ERRORES COMUNES DEL TEOREMA DE PITÁGORAS
  # Generar distractores pedagógicos específicos para cada configuración

  # Calcular valores que representan errores típicos
  hipotenusa_cuadrada <- hipotenusa^2
  cateto_conocido_cuadrado <- cateto_conocido^2

  # Pool amplio de distractores matemáticamente plausibles
  distractores_pool <- list()

  # Tipo 1: Error - usar la hipotenusa como respuesta
  distractores_pool[[1]] <- list(
    valor = hipotenusa,
    texto = formato_numero(hipotenusa),
    justificacion = "confunde hipotenusa con cateto"
  )

  # Tipo 2: Error - usar el cateto conocido como respuesta
  distractores_pool[[2]] <- list(
    valor = cateto_conocido,
    texto = formato_numero(cateto_conocido),
    justificacion = "confunde catetos entre sí"
  )

  # Tipo 3: Error - sumar en lugar de restar (c² + a² en lugar de c² - a²)
  valor_suma_error <- sqrt(hipotenusa_cuadrada + cateto_conocido_cuadrado)
  distractores_pool[[3]] <- list(
    valor = valor_suma_error,
    texto = formato_numero(valor_suma_error),
    justificacion = "suma incorrectamente en lugar de restar"
  )

  # Tipo 4: Error - usar solo la diferencia sin raíz cuadrada
  valor_sin_raiz <- hipotenusa_cuadrada - cateto_conocido_cuadrado
  distractores_pool[[4]] <- list(
    valor = valor_sin_raiz,
    texto = formato_numero(valor_sin_raiz),
    justificacion = "olvida aplicar la raíz cuadrada"
  )

  # Tipo 5: Error - intercambiar operaciones (a² - c² en lugar de c² - a²)
  if (cateto_conocido_cuadrado > hipotenusa_cuadrada) {
    valor_intercambio <- sqrt(abs(cateto_conocido_cuadrado - hipotenusa_cuadrada))
  } else {
    valor_intercambio <- cateto_conocido / 2  # Valor alternativo
  }
  distractores_pool[[5]] <- list(
    valor = valor_intercambio,
    texto = formato_numero(valor_intercambio),
    justificacion = "intercambia términos en la ecuación"
  )

  # Tipo 6: Error - usar la mitad de la hipotenusa
  distractores_pool[[6]] <- list(
    valor = hipotenusa / 2,
    texto = formato_numero(hipotenusa / 2),
    justificacion = "divide incorrectamente la hipotenusa"
  )

  # Tipo 7: Error - usar el doble del cateto conocido
  distractores_pool[[7]] <- list(
    valor = cateto_conocido * 2,
    texto = formato_numero(cateto_conocido * 2),
    justificacion = "duplica incorrectamente el cateto conocido"
  )

  # Tipo 8: Error - promedio de cateto conocido e hipotenusa
  valor_promedio <- (cateto_conocido + hipotenusa) / 2
  distractores_pool[[8]] <- list(
    valor = valor_promedio,
    texto = formato_numero(valor_promedio),
    justificacion = "calcula promedio en lugar de aplicar teorema"
  )

  # Tipo 9: Error - usar raíz de la suma simple
  valor_raiz_suma <- sqrt(cateto_conocido + hipotenusa)
  distractores_pool[[9]] <- list(
    valor = valor_raiz_suma,
    texto = formato_numero(valor_raiz_suma),
    justificacion = "aplica raíz a suma simple"
  )

  # Tipo 10: Error - valor basado en factor de diversidad
  valor_diversidad <- abs(cateto_desconocido + (factor_diversidad %% 5) * 0.5 - 1)
  distractores_pool[[10]] <- list(
    valor = valor_diversidad,
    texto = formato_numero(valor_diversidad),
    justificacion = "error de cálculo numérico"
  )


  


  # SISTEMA MEJORADO DE SELECCIÓN DE DISTRACTORES ÚNICOS
  # Filtrar distractores que sean diferentes del valor correcto
  distractores_validos <- list()
  for (i in seq_along(distractores_pool)) {
    distractor <- distractores_pool[[i]]
    # Verificar que el distractor sea diferente del valor correcto
    if (abs(distractor$valor - cateto_desconocido) > 0.01) {
      distractores_validos <- append(distractores_validos, list(distractor))
    }
  }

  # Si no hay suficientes distractores válidos, agregar algunos genéricos
  if (length(distractores_validos) < 3) {
    distractores_genericos <- list(
      list(valor = cateto_desconocido * 1.5,
           texto = formato_numero(cateto_desconocido * 1.5),
           justificacion = "error de cálculo"),
      list(valor = cateto_desconocido * 0.5,
           texto = formato_numero(cateto_desconocido * 0.5),
           justificacion = "error de cálculo"),
      list(valor = cateto_desconocido + 1,
           texto = formato_numero(cateto_desconocido + 1),
           justificacion = "error aritmético"),
      list(valor = abs(cateto_desconocido - 1),
           texto = formato_numero(abs(cateto_desconocido - 1)),
           justificacion = "error aritmético")
    )

    for (generico in distractores_genericos) {
      if (abs(generico$valor - cateto_desconocido) > 0.01 &&
          length(distractores_validos) < 10) {
        distractores_validos <- append(distractores_validos, list(generico))
      }
    }
  }

  # Seleccionar 3 distractores únicos aleatoriamente
  set.seed(seed_distractores)
  indices_seleccionados <- sample(seq_along(distractores_validos),
                                  min(3, length(distractores_validos)),
                                  replace = FALSE)

  # Construir opciones garantizando unicidad
  opciones <- list(
    list(valor = cateto_desconocido,
         texto = cateto_desconocido_tex,
         correcta = TRUE,
         justificacion = "respuesta correcta")
  )

  # Agregar distractores seleccionados
  for (i in indices_seleccionados) {
    distractores_validos[[i]]$correcta <- FALSE
    opciones <- append(opciones, list(distractores_validos[[i]]))
  }

  # VERIFICACIÓN CRÍTICA: Garantizar que tenemos exactamente 4 opciones
  while (length(opciones) < 4) {
    # Agregar distractor adicional si es necesario
    valor_adicional <- cateto_desconocido + sample(c(-2, -1, 1, 2), 1) * 0.5
    if (valor_adicional > 0 &&
        !any(sapply(opciones, function(x) abs(x$valor - valor_adicional) < 0.01))) {
      opciones <- append(opciones, list(list(
        valor = valor_adicional,
        texto = formato_numero(valor_adicional),
        correcta = FALSE,
        justificacion = "error de cálculo"
      )))
    }
  }

  # Tomar solo las primeras 4 opciones si hay más
  opciones <- opciones[1:4]

  # VERIFICACIÓN CRÍTICA: Garantizar que no hay opciones duplicadas
  textos_opciones <- sapply(opciones, function(x) x$texto)
  if (length(unique(textos_opciones)) != 4) {
    stop("ERROR CRÍTICO: Se detectaron opciones de respuesta duplicadas")
  }

  # Aleatorizar orden de opciones
  orden_opciones <- sample(1:4)
  opciones_ordenadas <- opciones[orden_opciones]

  # VERIFICACIÓN CRÍTICA: Garantizar que hay exactamente una respuesta correcta
  respuestas_correctas <- sapply(opciones_ordenadas, function(x) x$correcta)
  num_correctas <- sum(respuestas_correctas)
  if (num_correctas != 1) {
    stop(paste("ERROR CRÍTICO: Se encontraron", num_correctas,
               "respuestas correctas. Debe haber exactamente 1."))
  }

  # Encontrar la posición de la respuesta correcta
  posicion_correcta <- which(respuestas_correctas)
  solucion <- c("0", "0", "0", "0")
  solucion[posicion_correcta] <- "1"

  list(
    contexto = contexto_seleccionado,
    tipo_representacion = tipo_representacion,
    cateto_conocido = cateto_conocido,
    cateto_conocido_tex = cateto_conocido_tex,
    cateto_desconocido = cateto_desconocido,
    cateto_desconocido_tex = cateto_desconocido_tex,
    hipotenusa = hipotenusa,
    hipotenusa_tex = hipotenusa_tex,
    configuracion = configuracion,
    opciones = opciones_ordenadas,
    solucion = paste(solucion, collapse = ""),
    diversidad_info = list(
      num_contextos = length(contextos_problema),
      num_ternas = length(ternas_pitagoricas),
      num_configuraciones = 2,
      num_tipos_distractor = 10,
      num_representaciones = 3,
      factor_diversidad = factor_diversidad,
      variacion_contexto = variacion_contexto,
      seed_usado = seed_distractores,
      combinaciones_posibles = length(contextos_problema) * length(ternas_pitagoricas) * 2 * 10 * 3
    )
  )
}

# Generar datos del ejercicio
datos <- generar_datos()

# Extraer variables para facilitar uso
cateto_conocido <- datos$cateto_conocido
cateto_conocido_tex <- datos$cateto_conocido_tex
cateto_desconocido_tex <- datos$cateto_desconocido_tex
hipotenusa_tex <- datos$hipotenusa_tex
configuracion <- datos$configuracion
opciones <- datos$opciones
solucion <- datos$solucion
```

```{r version_diversity_test, echo=FALSE, results="hide"}
# Prueba obligatoria de diversidad de versiones
tryCatch({
  versiones <- list()
  for(i in 1:500) {  # Reducido para evitar timeout
    datos_test <- generar_datos()
    versiones[[i]] <- digest::digest(datos_test)
  }

  n_versiones_unicas <- length(unique(versiones))

  if(n_versiones_unicas >= 300) {
    cat("✅ Prueba de diversidad EXITOSA:", n_versiones_unicas, "versiones únicas\n")
  } else {
    warning(paste("Solo se generaron", n_versiones_unicas,
                  "versiones únicas. Se requieren al menos 300."))
  }
}, error = function(e) {
  warning("Error en prueba de diversidad:", e$message)
})
```

```{r generar_tikz, echo=FALSE, results="hide"}
# GRÁFICA TikZ DINÁMICA ADAPTADA A VALORES ALEATORIOS
# Representa triángulo rectángulo con valores variables
# Mantiene coherencia educativa y claridad visual

# Determinar qué cateto es el conocido y cuál el desconocido
if (configuracion == "a_conocido") {
  cateto_horizontal <- cateto_conocido
  cateto_horizontal_tex <- cateto_conocido_tex
  cateto_vertical_tex <- "x"
  etiqueta_horizontal <- cateto_conocido_tex
  etiqueta_vertical <- "x"
} else {
  cateto_horizontal_tex <- "x"
  cateto_vertical <- cateto_conocido
  cateto_vertical_tex <- cateto_conocido_tex
  etiqueta_horizontal <- "x"
  etiqueta_vertical <- cateto_conocido_tex
}

# Crear gráfico TikZ dinámico
tikz_triangulo_dinamico <- paste0('
\\begin{tikzpicture}[scale=2.5]
  % Marco exterior para claridad visual
  \\draw[black, thick] (-0.5,-0.5) rectangle (3.5,3.5);

  % TRIÁNGULO RECTÁNGULO PRINCIPAL
  % Cateto horizontal
  \\draw[black, very thick] (0,0) -- (3,0);
  % Cateto vertical
  \\draw[black, very thick] (0,0) -- (0,3);
  % Hipotenusa
  \\draw[black, very thick] (0,3) -- (3,0);

  % Símbolo de ángulo recto
  \\draw[black, thick] (0.3,0) -- (0.3,0.3) -- (0,0.3);

  % ETIQUETAS DINÁMICAS
  % Etiqueta del cateto horizontal (parte inferior)
  \\node[black, font=\\Large\\bfseries] at (1.5,-0.4) {$', etiqueta_horizontal, '$};

  % Etiqueta del cateto vertical (lado izquierdo)
  \\node[black, font=\\Large\\bfseries] at (-0.4,1.5) {$', etiqueta_vertical, '$};

  % Etiqueta de la hipotenusa (centro del lado inclinado)
  \\node[black, font=\\Large\\bfseries] at (1.8,1.8) {$', hipotenusa_tex, '$};

  % Punto de origen
  \\fill[black] (0,0) circle (0.05);
  \\fill[black] (3,0) circle (0.05);
  \\fill[black] (0,3) circle (0.05);
\\end{tikzpicture}
')
```

Question
========

`r datos$contexto`.

```{r mostrar_tikz, echo=FALSE, results="asis"}
# Renderizar con include_tikz siguiendo patrón de ejemplos funcionales
include_tikz(tikz_triangulo_dinamico,
             name = "triangulo_pitagoras",
             markup = "markdown",
             format = typ,
             library = c("babel", "calc", "3d"),
             packages = c("tikz", "xcolor"),
             width = "8cm")
```

Para resolver este problema, aplica correctamente el **Teorema de Pitágoras**:

En un triángulo rectángulo con catetos de longitud $`r if(configuracion == "a_conocido") cateto_conocido_tex else "x"`$ y $`r if(configuracion == "a_conocido") "x" else cateto_conocido_tex`$, y con hipotenusa de longitud $`r hipotenusa_tex`$, se cumple que:

$$(`r hipotenusa_tex`)^2 = (`r if(configuracion == "a_conocido") cateto_conocido_tex else "x"`)^2 + (`r if(configuracion == "a_conocido") "x" else cateto_conocido_tex`)^2$$

¿Cuál es el valor del cateto $x$?

Answerlist
----------
* $x = `r opciones[[1]]$texto`$
* $x = `r opciones[[2]]$texto`$
* $x = `r opciones[[3]]$texto`$
* $x = `r opciones[[4]]$texto`$

Solution
========

Para resolver este problema, aplicamos el **Teorema de Pitágoras** de manera sistemática.

**Análisis de la figura:**

- El triángulo rectángulo tiene catetos de longitud $`r if(configuracion == "a_conocido") cateto_conocido_tex else "x"`$ y $`r if(configuracion == "a_conocido") "x" else cateto_conocido_tex`$
- La hipotenusa del triángulo es $`r hipotenusa_tex`$
- Debemos encontrar el valor de $x$

**Aplicación del Teorema de Pitágoras:**

En un triángulo rectángulo: $\text{hipotenusa}^2 = \text{cateto}_1^2 + \text{cateto}_2^2$

Sustituyendo los valores conocidos:
$$(`r hipotenusa_tex`)^2 = (`r if(configuracion == "a_conocido") cateto_conocido_tex else "x"`)^2 + (`r if(configuracion == "a_conocido") "x" else cateto_conocido_tex`)^2$$

**Resolución paso a paso:**

```{r calculos_solucion, echo=FALSE, results="asis"}
# Calcular los valores para la solución
hipotenusa_cuadrada <- datos$hipotenusa^2
cateto_conocido_cuadrado <- datos$cateto_conocido^2

if (configuracion == "a_conocido") {
  cat("Calculamos el cuadrado de la hipotenusa: $(", hipotenusa_tex, ")^2 = ", hipotenusa_cuadrada, "$\n\n")
  cat("Calculamos el cuadrado del cateto conocido: $(", cateto_conocido_tex, ")^2 = ", cateto_conocido_cuadrado, "$\n\n")
  cat("Despejamos $x^2$: $x^2 = ", hipotenusa_cuadrada, " - ", cateto_conocido_cuadrado, " = ", hipotenusa_cuadrada - cateto_conocido_cuadrado, "$\n\n")
  cat("Por lo tanto: $x = \\sqrt{", hipotenusa_cuadrada - cateto_conocido_cuadrado, "} = ", cateto_desconocido_tex, "$\n\n")
} else {
  cat("Calculamos el cuadrado de la hipotenusa: $(", hipotenusa_tex, ")^2 = ", hipotenusa_cuadrada, "$\n\n")
  cat("Calculamos el cuadrado del cateto conocido: $(", cateto_conocido_tex, ")^2 = ", cateto_conocido_cuadrado, "$\n\n")
  cat("Despejamos $x^2$: $x^2 = ", hipotenusa_cuadrada, " - ", cateto_conocido_cuadrado, " = ", hipotenusa_cuadrada - cateto_conocido_cuadrado, "$\n\n")
  cat("Por lo tanto: $x = \\sqrt{", hipotenusa_cuadrada - cateto_conocido_cuadrado, "} = ", cateto_desconocido_tex, "$\n\n")
}
```

**Verificación:**
Si $x = `r cateto_desconocido_tex`$, entonces:
$$\sqrt{(`r if(configuracion == "a_conocido") cateto_conocido_tex else cateto_desconocido_tex`)^2 + (`r if(configuracion == "a_conocido") cateto_desconocido_tex else cateto_conocido_tex`)^2} = \sqrt{`r cateto_conocido_cuadrado` + `r datos$cateto_desconocido^2`} = \sqrt{`r hipotenusa_cuadrada`} = `r hipotenusa_tex`$$

La verificación confirma que $x = `r cateto_desconocido_tex`$ es la respuesta correcta.

Answerlist
----------
* `r if(opciones[[1]]$correcta) "Verdadero" else "Falso"`. `r if(opciones[[1]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras, el cateto desconocido es x =", cateto_desconocido_tex) else paste("Error:", opciones[[1]]$justificacion)`
* `r if(opciones[[2]]$correcta) "Verdadero" else "Falso"`. `r if(opciones[[2]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras, el cateto desconocido es x =", cateto_desconocido_tex) else paste("Error:", opciones[[2]]$justificacion)`
* `r if(opciones[[3]]$correcta) "Verdadero" else "Falso"`. `r if(opciones[[3]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras, el cateto desconocido es x =", cateto_desconocido_tex) else paste("Error:", opciones[[3]]$justificacion)`
* `r if(opciones[[4]]$correcta) "Verdadero" else "Falso"`. `r if(opciones[[4]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras, el cateto desconocido es x =", cateto_desconocido_tex) else paste("Error:", opciones[[4]]$justificacion)`

Meta-information
================
exname: Cateto triangulo rectangulo teorema Pitagoras
extype: schoice
exsolution: `r solucion`
exshuffle: TRUE
exsection: Geometria/Teorema de Pitagoras

# Metadatos ICFES obligatorios
icfes:
  competencia: formulacion_ejecucion
  nivel_dificultad: 2
  contenido:
    categoria: geometria
    tipo: generico
  contexto: matematico
  eje_axial: eje2
  componente: geometrico_metrico
