---
output:
  word_document: default
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
# Configuración para todos los formatos de salida
Sys.setlocale(category = "LC_NUMERIC", locale = "C")
options(OutDec = ".")

# Configurar el motor LaTeX globalmente
options(tikzLatex = "pdflatex")
options(tikzXelatex = FALSE)
options(tikzLatexPackages = c(
  "\\usepackage{tikz}",
  "\\usepackage{colortbl}",
  "\\usepackage{amsmath}",
  "\\usepackage{array}",
  "\\usepackage{xcolor}",
  "\\usepackage{graphicx}",
  "\\usepackage{float}",
  "\\usetikzlibrary{babel,calc,3d}"
))

library(exams)
library(digest)
library(testthat)

typ <- match_exams_device()
options(scipen = 999)
knitr::opts_chunk$set(
  warning = FALSE,
  message = FALSE,
  fig.showtext = FALSE,
  fig.cap = "",
  fig.keep = 'all',
  dev = c("png", "pdf"),
  dpi = 150
)
```

```{r data_generation, echo=FALSE, results="hide"}
# Configuración de formato numérico estándar
options(OutDec = ".")
options(scipen = 999)
options(digits = 10)

# Establecer semilla aleatoria para reproducibilidad
#set.seed(sample(1:100000, 1))
#set.seed(101) # 10A

# Función principal de generación de datos
generar_datos <- function() {
  # NUEVA IMPLEMENTACIÓN: Aleatorización de catetos manteniendo filosofía ICFES
  # Objetivo: Generar valores que produzcan cálculos simples sin calculadora

  # Generar semilla única para esta ejecución
  timestamp_seed <- as.numeric(Sys.time()) * 1000000
  base_seed <- sample(1:1000000, 1)
  unique_seed <- (timestamp_seed + base_seed) %% 1000000

  # Aleatorizar contextos del problema para mayor diversidad
  contextos_problema <- c(
    "Margarita debe encontrar el valor del cateto x en el triángulo rectángulo que se muestra en la figura",
    "Carlos necesita determinar el valor del cateto x del triángulo rectángulo representado en el diagrama",
    "Ana debe calcular el valor del cateto x en el triángulo rectángulo que aparece en la imagen",
    "Luis requiere hallar el valor del cateto x del triángulo rectángulo mostrado en la figura",
    "María debe encontrar el valor del cateto x en el triángulo rectángulo que se presenta en el gráfico",
    "Sofía necesita calcular el valor del cateto x en el triángulo rectángulo de la figura",
    "Diego debe determinar el valor del cateto x del triángulo rectángulo mostrado",
    "Elena requiere encontrar el valor del cateto x en el triángulo rectángulo del diagrama",
    "Pablo debe hallar el valor del cateto x del triángulo rectángulo representado",
    "Carmen necesita calcular el valor del cateto x en el triángulo rectángulo de la imagen"
  )
  contexto_seleccionado <- sample(contextos_problema, 1)

  # ALEATORIZACIÓN INTELIGENTE DE CATETOS MANTENIENDO FILOSOFÍA ICFES
  # Usar ternas pitagóricas simples que permitan cálculos mentales sin calculadora
  # Mantener la estructura del enunciado pero variar los valores numéricos

  # Ternas pitagóricas básicas y escaladas para cálculos simples ICFES
  ternas_pitagoricas <- list(
    # Ternas básicas clásicas
    c(3, 4, 5),     # La más conocida
    c(5, 12, 13),   # Segunda más conocida
    c(8, 15, 17),   # Tercera clásica
    c(7, 24, 25),   # Cuarta clásica
    c(6, 8, 10),    # Múltiplo de (3,4,5)
    c(9, 12, 15),   # Múltiplo de (3,4,5)
    c(12, 16, 20),  # Múltiplo de (3,4,5)

    # Ternas con números más pequeños para facilitar cálculos
    c(1, 1, sqrt(2)),  # Caso especial original
    c(2, 2, sqrt(8)),  # Múltiplo del original

    # Ternas con decimales simples
    c(1.5, 2, 2.5),    # Múltiplo de (3,4,5) ÷ 2
    c(2.5, 6, 6.5),    # Múltiplo de (5,12,13) ÷ 2
    c(0.6, 0.8, 1),    # Múltiplo de (3,4,5) ÷ 5
    c(1.2, 1.6, 2),    # Múltiplo de (3,4,5) ÷ 2.5

    # Casos especiales con raíces simples
    c(1, 2, sqrt(5)),  # Raíz de 5
    c(2, 3, sqrt(13)), # Raíz de 13
    c(1, 3, sqrt(10))  # Raíz de 10
  )

  # Seleccionar terna aleatoriamente
  terna_seleccionada <- sample(ternas_pitagoricas, 1)[[1]]

  # Decidir aleatoriamente cuál cateto es el conocido y cuál el desconocido
  configuracion <- sample(c("a_conocido", "b_conocido"), 1)

  if (configuracion == "a_conocido") {
    cateto_conocido <- terna_seleccionada[1]
    cateto_desconocido <- terna_seleccionada[2]
    hipotenusa <- terna_seleccionada[3]
  } else {
    cateto_conocido <- terna_seleccionada[2]
    cateto_desconocido <- terna_seleccionada[1]
    hipotenusa <- terna_seleccionada[3]
  }

  # Aleatorizar factores de diversidad para generar 300+ versiones
  factor_diversidad <- sample(1:1000, 1)  # Factor ampliado
  variacion_contexto <- sample(1:100, 1)  # Variación ampliada
  seed_distractores <- unique_seed + factor_diversidad + variacion_contexto

  # Función de formateo inteligente para diferentes tipos de números
  formato_numero <- function(x, tipo_rep = "exacta") {
    # Números enteros
    if (abs(x - round(x)) < 0.001 && x <= 20) {
      return(as.character(round(x)))
    }

    # Decimales simples (una cifra decimal)
    if (abs(x - round(x, 1)) < 0.001 && x < 10) {
      return(as.character(round(x, 1)))
    }

    # Fracciones comunes
    if (abs(x - 1/2) < 0.001) return("\\frac{1}{2}")
    if (abs(x - 3/2) < 0.001) return("\\frac{3}{2}")
    if (abs(x - 5/2) < 0.001) return("\\frac{5}{2}")
    if (abs(x - 2/3) < 0.001) return("\\frac{2}{3}")
    if (abs(x - 4/3) < 0.001) return("\\frac{4}{3}")
    if (abs(x - 3/4) < 0.001) return("\\frac{3}{4}")
    if (abs(x - 5/4) < 0.001) return("\\frac{5}{4}")

    # Raíces comunes (verificar orden de precedencia)
    if (abs(x - 2*sqrt(2)) < 0.001) return("2\\sqrt{2}")  # sqrt(8) = 2*sqrt(2)
    if (abs(x - sqrt(2)) < 0.001) return("\\sqrt{2}")
    if (abs(x - sqrt(3)) < 0.001) return("\\sqrt{3}")
    if (abs(x - sqrt(5)) < 0.001) return("\\sqrt{5}")
    if (abs(x - sqrt(10)) < 0.001) return("\\sqrt{10}")
    if (abs(x - sqrt(13)) < 0.001) return("\\sqrt{13}")
    if (abs(x - 3*sqrt(2)) < 0.001) return("3\\sqrt{2}")
    if (abs(x - sqrt(18)) < 0.001) return("3\\sqrt{2}")  # sqrt(18) = 3*sqrt(2)

    # Formato por defecto
    return(as.character(round(x, 2)))
  }

  # Función específica para TikZ que necesita escape adicional
  formato_numero_tikz <- function(x) {
    # Números enteros
    if (abs(x - round(x)) < 0.001 && x <= 20) {
      return(as.character(round(x)))
    }

    # Raíces comunes para TikZ - doble escape (verificar orden de precedencia)
    if (abs(x - 2 * sqrt(2)) < 0.001) return("2\\\\sqrt{2}")  # sqrt(8) = 2*sqrt(2)
    if (abs(x - sqrt(2)) < 0.001) return("\\\\sqrt{2}")
    if (abs(x - sqrt(3)) < 0.001) return("\\\\sqrt{3}")
    if (abs(x - sqrt(5)) < 0.001) return("\\\\sqrt{5}")
    if (abs(x - sqrt(10)) < 0.001) return("\\\\sqrt{10}")
    if (abs(x - sqrt(13)) < 0.001) return("\\\\sqrt{13}")
    if (abs(x - 3 * sqrt(2)) < 0.001) return("3\\\\sqrt{2}")  # sqrt(18) = 3*sqrt(2)

    # Formato por defecto
    return(as.character(round(x, 2)))
  }

  # Formatear valores principales
  cateto_conocido_tex <- formato_numero(cateto_conocido)
  cateto_desconocido_tex <- formato_numero(cateto_desconocido)
  hipotenusa_tex <- formato_numero(hipotenusa)

  # Formatear valores para TikZ (con escape adicional)
  cateto_conocido_tikz <- formato_numero_tikz(cateto_conocido)
  hipotenusa_tikz <- formato_numero_tikz(hipotenusa)

  # Usar seed_distractores para crear variaciones únicas
  set.seed(seed_distractores)

  # GENERACIÓN DE DISTRACTORES DINÁMICOS BASADOS EN LA TERNA SELECCIONADA
  # Adaptados a los valores específicos de cada ejercicio
  distractores_pool <- list()

  # Tipo 1: Confundir hipotenusa con cateto desconocido
  distractores_pool[[1]] <- list(
    valor = hipotenusa,
    texto = formato_numero(hipotenusa),
    justificacion = "confunde hipotenusa con cateto"
  )

  # Tipo 2: Usar el cateto conocido como respuesta
  distractores_pool[[2]] <- list(
    valor = cateto_conocido,
    texto = formato_numero(cateto_conocido),
    justificacion = "confunde cateto conocido con desconocido"
  )

  # Tipo 3: Error aritmético - suma en lugar de restar
  if (all(hipotenusa^2 + cateto_conocido^2 > 0)) {
    valor_suma_error <- sqrt(hipotenusa^2 + cateto_conocido^2)
    distractores_pool[[3]] <- list(
      valor = valor_suma_error,
      texto = formato_numero(valor_suma_error),
      justificacion = "suma en lugar de restar"
    )
  }

  # Tipo 4: Error - olvidar aplicar raíz cuadrada
  valor_sin_raiz <- hipotenusa^2 - cateto_conocido^2
  if (all(valor_sin_raiz > 0)) {
    distractores_pool[[4]] <- list(
      valor = valor_sin_raiz,
      texto = formato_numero(valor_sin_raiz),
      justificacion = "olvida aplicar raíz cuadrada"
    )
  }

  # Tipo 5: Error - promedio aritmético
  valor_promedio <- (hipotenusa + cateto_conocido) / 2
  distractores_pool[[5]] <- list(
    valor = valor_promedio,
    texto = formato_numero(valor_promedio),
    justificacion = "calcula promedio incorrecto"
  )

  # Tipo 6: Error - fracción del cateto conocido
  valor_fraccion <- cateto_conocido * 0.75
  distractores_pool[[6]] <- list(
    valor = valor_fraccion,
    texto = formato_numero(valor_fraccion),
    justificacion = "usa fracción del cateto conocido"
  )

  # Tipo 7: Error - doble del cateto conocido
  valor_doble <- cateto_conocido * 2
  distractores_pool[[7]] <- list(
    valor = valor_doble,
    texto = formato_numero(valor_doble),
    justificacion = "duplica el cateto conocido"
  )

  # Tipo 8: Error - diferencia simple
  valor_diferencia <- abs(hipotenusa - cateto_conocido)
  distractores_pool[[8]] <- list(
    valor = valor_diferencia,
    texto = formato_numero(valor_diferencia),
    justificacion = "resta simple sin teorema"
  )

  # Tipo 9: Error - valor aleatorio basado en diversidad
  valores_base <- c(0.5, 0.75, 1.25, 1.5, 1.75, 2.25, 2.5, 3.5, 4.5)
  valor_diversidad <- sample(valores_base, 1)
  distractores_pool[[9]] <- list(
    valor = valor_diversidad,
    texto = formato_numero(valor_diversidad),
    justificacion = "error de cálculo numérico"
  )

  # Tipo 10: Error - mitad de la hipotenusa
  valor_mitad <- hipotenusa / 2
  distractores_pool[[10]] <- list(
    valor = valor_mitad,
    texto = formato_numero(valor_mitad),
    justificacion = "usa mitad de la hipotenusa"
  )


  


  # SISTEMA MEJORADO DE SELECCIÓN DE DISTRACTORES ÚNICOS
  # Filtrar distractores que sean diferentes del valor correcto
  distractores_validos <- list()
  for (i in seq_along(distractores_pool)) {
    distractor <- distractores_pool[[i]]
    # Verificar que el distractor sea diferente del valor correcto
    if (abs(distractor$valor - cateto_desconocido) > 0.01) {
      distractores_validos <- append(distractores_validos, list(distractor))
    }
  }

  # Si no hay suficientes distractores válidos, agregar algunos genéricos
  if (length(distractores_validos) < 3) {
    distractores_genericos <- list(
      list(valor = cateto_desconocido * 1.5,
           texto = formato_numero(cateto_desconocido * 1.5),
           justificacion = "error de cálculo"),
      list(valor = cateto_desconocido * 0.5,
           texto = formato_numero(cateto_desconocido * 0.5),
           justificacion = "error de cálculo"),
      list(valor = cateto_desconocido + 1,
           texto = formato_numero(cateto_desconocido + 1),
           justificacion = "error aritmético"),
      list(valor = abs(cateto_desconocido - 1),
           texto = formato_numero(abs(cateto_desconocido - 1)),
           justificacion = "error aritmético")
    )

    for (generico in distractores_genericos) {
      if (abs(generico$valor - cateto_desconocido) > 0.01 &&
          length(distractores_validos) < 10) {
        distractores_validos <- append(distractores_validos, list(generico))
      }
    }
  }

  # Seleccionar 3 distractores únicos aleatoriamente
  set.seed(seed_distractores)
  indices_seleccionados <- sample(seq_along(distractores_validos),
                                  min(3, length(distractores_validos)),
                                  replace = FALSE)

  # Construir opciones garantizando unicidad
  opciones <- list(
    list(valor = cateto_desconocido,
         texto = cateto_desconocido_tex,
         correcta = TRUE,
         justificacion = "respuesta correcta")
  )

  # Agregar distractores seleccionados
  for (i in indices_seleccionados) {
    distractores_validos[[i]]$correcta <- FALSE
    opciones <- append(opciones, list(distractores_validos[[i]]))
  }

  # VERIFICACIÓN CRÍTICA: Garantizar que tenemos exactamente 4 opciones
  while (length(opciones) < 4) {
    # Agregar distractor adicional si es necesario
    valor_adicional <- cateto_desconocido + sample(c(-2, -1, 1, 2), 1) * 0.5
    if (valor_adicional > 0 &&
        !any(sapply(opciones, function(x) abs(x$valor - valor_adicional) < 0.01))) {
      opciones <- append(opciones, list(list(
        valor = valor_adicional,
        texto = formato_numero(valor_adicional),
        correcta = FALSE,
        justificacion = "error de cálculo"
      )))
    }
  }

  # Tomar solo las primeras 4 opciones si hay más
  opciones <- opciones[1:4]

  # VERIFICACIÓN CRÍTICA: Garantizar que no hay opciones duplicadas
  textos_opciones <- sapply(opciones, function(x) x$texto)
  valores_opciones <- sapply(opciones, function(x) x$valor)

  # Verificar unicidad por TEXTO (más crítico) y valor numérico
  textos_unicos <- length(unique(textos_opciones))
  valores_unicos <- length(unique(round(valores_opciones, 3)))

  if (textos_unicos != 4 || valores_unicos != 4) {
    cat("ADVERTENCIA: Opciones duplicadas detectadas!\n")
    cat("Textos únicos:", textos_unicos, "- Valores únicos:", valores_unicos, "\n")
    cat("Textos:", paste(textos_opciones, collapse = ", "), "\n")

    # Regenerar opciones con valores garantizadamente diferentes
    opciones <- list(
      list(valor = cateto_desconocido, texto = cateto_desconocido_tex, correcta = TRUE, justificacion = "respuesta correcta"),
      list(valor = cateto_conocido, texto = cateto_conocido_tex, correcta = FALSE, justificacion = "confunde catetos"),
      list(valor = hipotenusa, texto = hipotenusa_tex, correcta = FALSE, justificacion = "confunde con hipotenusa"),
      list(valor = cateto_desconocido + 1, texto = formato_numero(cateto_desconocido + 1), correcta = FALSE, justificacion = "error aritmético")
    )
  }

  # Aleatorizar orden de opciones
  orden_opciones <- sample(1:4)
  opciones_ordenadas <- opciones[orden_opciones]

  # VERIFICACIÓN CRÍTICA: Garantizar que hay exactamente una respuesta correcta
  respuestas_correctas <- sapply(opciones_ordenadas, function(x) x$correcta)
  num_correctas <- sum(respuestas_correctas)
  if (num_correctas != 1) {
    stop(paste("ERROR CRÍTICO: Se encontraron", num_correctas,
               "respuestas correctas. Debe haber exactamente 1."))
  }

  # Encontrar la posición de la respuesta correcta
  posicion_correcta <- which(respuestas_correctas)
  solucion <- c("0", "0", "0", "0")
  solucion[posicion_correcta] <- "1"

  list(
    contexto = contexto_seleccionado,
    cateto_conocido = cateto_conocido,
    cateto_conocido_tex = cateto_conocido_tex,
    cateto_conocido_tikz = cateto_conocido_tikz,
    cateto_desconocido = cateto_desconocido,
    cateto_desconocido_tex = cateto_desconocido_tex,
    hipotenusa = hipotenusa,
    hipotenusa_tex = hipotenusa_tex,
    hipotenusa_tikz = hipotenusa_tikz,
    configuracion = configuracion,
    opciones = opciones_ordenadas,
    solucion = paste(solucion, collapse = ""),
    diversidad_info = list(
      num_contextos = length(contextos_problema),
      num_tipos_distractor = 10,
      num_representaciones = 3,
      factor_diversidad = factor_diversidad,
      variacion_contexto = variacion_contexto,
      seed_usado = seed_distractores,
      combinaciones_posibles = length(contextos_problema) * 10 * 3 * 100 * 20
    )
  )
}

# Generar datos del ejercicio
datos <- generar_datos()

# Extraer variables para facilitar uso
cateto_conocido <- datos$cateto_conocido
cateto_conocido_tex <- datos$cateto_conocido_tex
cateto_desconocido_tex <- datos$cateto_desconocido_tex
hipotenusa_tex <- datos$hipotenusa_tex
configuracion <- datos$configuracion
opciones <- datos$opciones
solucion <- datos$solucion
```

```{r version_diversity_test, echo=FALSE, results="hide"}
# Prueba obligatoria de diversidad de versiones
tryCatch({
  versiones <- list()
  for(i in 1:500) {  # Reducido para evitar timeout
    datos_test <- generar_datos()
    versiones[[i]] <- digest::digest(datos_test)
  }

  n_versiones_unicas <- length(unique(versiones))

  if(n_versiones_unicas >= 300) {
    cat("✅ Prueba de diversidad EXITOSA:", n_versiones_unicas, "versiones únicas\n")
  } else {
    warning(paste("Solo se generaron", n_versiones_unicas,
                  "versiones únicas. Se requieren al menos 300."))
  }
}, error = function(e) {
  warning("Error en prueba de diversidad:", e$message)
})
```

```{r generar_tikz, echo=FALSE, results="hide"}
# GRÁFICA TikZ CONSERVANDO ESTRUCTURA ORIGINAL
# Replica fielmente: cuadrado exterior, cuadrado rotado y triángulo rectángulo
# Mantiene coherencia con enunciado original y claridad educativa

# IMPORTANTE: Mantener estructura visual original pero con valores aleatorios
# El gráfico debe mostrar el cuadrado rotado con el triángulo rectángulo interno
# Conservando las proporciones y el diseño educativo original

tikz_cuadrado_rotado <- '
\\begin{tikzpicture}[scale=3]
  % Marco exterior negro grueso (como en la imagen original)
  \\draw[black, very thick] (-0.2,-0.2) rectangle (2.2,2.2);

  % Cuadrado exterior base (línea negra mediana)
  \\draw[black, thick] (0,0) rectangle (2,2);

  % CUADRADO ROTADO 45° (elemento principal de la imagen original)
  % Coordenadas calculadas para rotación de 45° centrada
  \\draw[black, very thick] (1,0) -- (2,1) -- (1,2) -- (0,1) -- cycle;

  % TRIÁNGULO RECTÁNGULO PRINCIPAL (esquina inferior izquierda)
  % Cateto horizontal (conocido)
  \\draw[black, very thick] (0,0) -- (1,0);
  % Cateto vertical (desconocido x)
  \\draw[black, very thick] (0,0) -- (0,1);
  % Hipotenusa
  \\draw[black, very thick] (0,1) -- (1,0);

  % Símbolo de ángulo recto (como en imagen original)
  \\draw[black, thick] (0.15,0) -- (0.15,0.15) -- (0,0.15);

  % ETIQUETAS DINÁMICAS BASADAS EN LA CONFIGURACIÓN
  % Etiqueta "x" en el cateto desconocido (lado izquierdo)
  \\node[black, font=\\Large\\bfseries] at (-0.3,0.5) {$x$};

  % Etiqueta del cateto conocido (parte inferior) - será reemplazada dinámicamente
  \\node[black, font=\\Large\\bfseries] at (0.5,-0.3) {CATETO_CONOCIDO};

  % Etiqueta de la hipotenusa (cerca de la línea diagonal) - será reemplazada dinámicamente
  \\node[black, font=\\Large\\bfseries] at (0.7,0.5) {HIPOTENUSA};
\\end{tikzpicture}
'
```

Question
========

`r datos$contexto`.

```{r mostrar_tikz, echo=FALSE, results="asis"}
# Hacer sustituciones dinámicas en el TikZ usando formato específico para TikZ
tikz_dinamico <- tikz_cuadrado_rotado
tikz_dinamico <- gsub("CATETO_CONOCIDO", paste0("$", datos$cateto_conocido_tikz, "$"), tikz_dinamico)
tikz_dinamico <- gsub("HIPOTENUSA", paste0("$", datos$hipotenusa_tikz, "$"), tikz_dinamico)

# Renderizar con include_tikz siguiendo patrón de ejemplos funcionales
include_tikz(tikz_dinamico,
             name = "cuadrado_rotado",
             markup = "markdown",
             format = typ,
             library = c("babel", "calc", "3d"),
             packages = c("tikz", "xcolor"),
             width = "8cm")
```

Para esto, efectúa correctamente el siguiente procedimiento:

**Paso 1.** Calcula la hipotenusa $\sqrt{x^2 + `r datos$cateto_conocido_tex`^2}$ y obtiene $`r datos$hipotenusa_tex`$.

**Paso 2.** El resultado del paso 1 lo eleva al cuadrado y obtiene como resultado el número `r datos$hipotenusa^2`.

¿Cuál es el valor del cateto $x$?

Answerlist
----------
* $x = `r opciones[[1]]$texto`$
* $x = `r opciones[[2]]$texto`$
* $x = `r opciones[[3]]$texto`$
* $x = `r opciones[[4]]$texto`$

Solution
========

Para resolver este problema, debemos analizar la información geométrica y aplicar el teorema de Pitágoras correctamente.

**Análisis de la figura:**

- El cuadrado exterior contiene el triángulo rectángulo
- El triángulo rectángulo tiene catetos de longitud $x$ y $`r datos$cateto_conocido_tex`$
- La hipotenusa del triángulo es $`r datos$hipotenusa_tex`$

**Resolución paso a paso:**

Del **Paso 1**: $\sqrt{x^2 + `r datos$cateto_conocido_tex`^2} = `r datos$hipotenusa_tex`$

Del **Paso 2**: $(`r datos$hipotenusa_tex`)^2 = `r datos$hipotenusa^2`$

Verificamos el **Paso 2**: $(`r datos$hipotenusa_tex`)^2 = `r datos$hipotenusa^2`$ (correcto)

Sustituyendo en la ecuación del **Paso 1**:
$x^2 + `r datos$cateto_conocido_tex`^2 = `r datos$hipotenusa^2`$

Resolviendo: $x^2 + `r datos$cateto_conocido^2` = `r datos$hipotenusa^2`$

Por lo tanto: $x^2 = `r datos$hipotenusa^2` - `r datos$cateto_conocido^2` = `r datos$cateto_desconocido^2`$

```{r calculos_solucion, echo=FALSE, results="asis"}
# Mostrar el cálculo específico para el valor de x generado
x_cuadrado <- datos$cateto_desconocido^2
cat("Para este ejercicio específico:\n\n")
cat("$$x^2 = ", datos$hipotenusa^2, " - ", datos$cateto_conocido^2, " = ", datos$cateto_desconocido^2, "$$\n\n")
cat("Entonces: $$x = \\sqrt{", datos$cateto_desconocido^2, "} = ", datos$cateto_desconocido_tex, "$$\n\n")
```

**Verificación geométrica:**
Si $x = `r datos$cateto_desconocido_tex`$, entonces $\sqrt{(`r datos$cateto_desconocido_tex`)^2 + (`r datos$cateto_conocido_tex`)^2} = \sqrt{`r datos$cateto_desconocido^2` + `r datos$cateto_conocido^2`} = \sqrt{`r datos$cateto_desconocido^2 + datos$cateto_conocido^2`} = `r datos$hipotenusa_tex`$

Y $(`r datos$hipotenusa_tex`)^2 = `r datos$hipotenusa^2`$ (verificado)

Answerlist
----------
* `r if(datos$opciones[[1]]$correcta) "Verdadero" else "Falso"`. `r if(datos$opciones[[1]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras: x² +", datos$cateto_conocido^2, "=", datos$hipotenusa^2, ", por lo tanto x =", datos$cateto_desconocido_tex) else paste("Error:", datos$opciones[[1]]$justificacion)`
* `r if(datos$opciones[[2]]$correcta) "Verdadero" else "Falso"`. `r if(datos$opciones[[2]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras: x² +", datos$cateto_conocido^2, "=", datos$hipotenusa^2, ", por lo tanto x =", datos$cateto_desconocido_tex) else paste("Error:", datos$opciones[[2]]$justificacion)`
* `r if(datos$opciones[[3]]$correcta) "Verdadero" else "Falso"`. `r if(datos$opciones[[3]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras: x² +", datos$cateto_conocido^2, "=", datos$hipotenusa^2, ", por lo tanto x =", datos$cateto_desconocido_tex) else paste("Error:", datos$opciones[[3]]$justificacion)`
* `r if(datos$opciones[[4]]$correcta) "Verdadero" else "Falso"`. `r if(datos$opciones[[4]]$correcta) paste("Aplicando correctamente el teorema de Pitágoras: x² +", datos$cateto_conocido^2, "=", datos$hipotenusa^2, ", por lo tanto x =", datos$cateto_desconocido_tex) else paste("Error:", datos$opciones[[4]]$justificacion)`

Meta-information
================
exname: Cateto triangulo rectangulo teorema Pitagoras
extype: schoice
exsolution: `r solucion`
exshuffle: TRUE
exsection: Geometria/Teorema de Pitagoras

# Metadatos ICFES obligatorios
icfes:
  competencia: formulacion_ejecucion
  nivel_dificultad: 2
  contenido:
    categoria: geometria
    tipo: generico
  contexto: matematico
  eje_axial: eje2
  componente: geometrico_metrico
